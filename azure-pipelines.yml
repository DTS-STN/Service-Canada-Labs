# Docker
# Build and push an image to Azure Container Registry
# https://docs.microsoft.com/azure/devops/pipelines/languages/docker
name: $(Date:yyyyMMdd)

trigger:
- master

resources:
- repo: self

variables:
  # Container registry service connection established during pipeline creation
  imageRepository: 'alphasite'
  ADO_BUILD: $(Build.BuildId)
  BUILD_DATE: $(Build.BuildNumber)
  tag: 'ADO-$(ADO_BUILD)'
  # Agent VM image name
  vmImageName: 'ubuntu-latest'

stages:
- stage: Build
  displayName: Build and push stage
  jobs:
  - job: Build
    displayName: Build and Push Container
    pool:
      vmImage: $(vmImageName)
    steps:
    - task: Docker@2
      inputs:
        containerRegistry: $(ACR_NAME)
        repository: $(imageRepository)
        command: 'build'
        Dockerfile: '**/Dockerfile'
        arguments: '--pull --build-arg BUILD_DATE=$(BUILD_DATE) TC_BUILD=$(ADO_BUILD) STRAPI_API_BACKEND_URL=$(STRAPI_API_BACKEND_URL)'
        tags: |
          $(tag)
    - task: Docker@2
      inputs:
        containerRegistry: $(ACR_NAME)
        repository: $(imageRepository)
        command: 'push'
        tags: |
          $(tag)

# - stage: Deploy
#   displayName: Deploy Container to App Service
#   jobs:  
#   - job: DeployAdmin
#     displayName: Deploy Admin Container
#     pool:
#       vmImage: $(vmImageName)
#     steps:
#     - task: AzureCLI@2
#       inputs:
#         azureSubscription: $(DECD_DEV_SUBSCRIPTION)
#         scriptType: 'bash'
#         scriptLocation: 'inlineScript'
#         inlineScript: 'az webapp config container set --docker-custom-image-name $(ACR_DOMAIN)/$(adminImageRepository):$(tag) --docker-registry-server-url $(ACR_DOMAIN) --name alphasite-strapi-admin-as-CanadaCentral --resource-group $(RESOURCE_GROUP)'
#     - task: AzureCLI@2
#       inputs:
#         azureSubscription: $(DECD_DEV_SUBSCRIPTION)
#         scriptType: 'bash'
#         scriptLocation: 'inlineScript'
#         inlineScript: 'az webapp config container set --docker-custom-image-name $(ACR_DOMAIN)/$(adminImageRepository):$(tag) --docker-registry-server-url $(ACR_DOMAIN) --name alphasite-strapi-admin-as-CanadaEast --resource-group $(RESOURCE_GROUP)'        