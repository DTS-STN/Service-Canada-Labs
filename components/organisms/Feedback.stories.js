import React from "react";
import { Feedback } from "./Feedback";

export default {
  title: "Components/Organisms/Feedback",
  component: Feedback,
};

const Template = (args) => <Feedback {...args} />;

export const Primary = Template.bind({});
